// print power
#include<stdio.h>
#include<math.h>
int main()
{
    int b,c;
    b =2, c =6;
    int a = b + c;
    // pow is the power
    int power = pow(b,c);
    printf("%d",power);
    return 0;
}